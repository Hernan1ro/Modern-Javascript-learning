// Pure functions

// reurn the same number of params that the function recives
const duplicar = (numero) => numero * 2;

const numero1 = 20;

const resultado = duplicar(numero1);

// It does not modify the variable
console.log(resultado);
console.log(numero1);
