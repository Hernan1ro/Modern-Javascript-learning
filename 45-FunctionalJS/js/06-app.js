// Pure functions

// reurn the same number of params that the function recives
const duplicar2 = (numero) => numero * 2;

const numero1 = 20;

const resultado2 = duplicar2(numero1);

// It does not modify the variable
console.log(resultado2);
console.log(numero1);
